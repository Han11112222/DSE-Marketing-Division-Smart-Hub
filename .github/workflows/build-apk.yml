name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-pip \
            build-essential \
            git \
            libsdl2-dev \
            libsdl2-image-dev \
            libsdl2-mixer-dev \
            libsdl2-ttf-dev \
            libportmidi-dev \
            zlib1g-dev \
            autoconf \
            libtool \
            pkg-config \
            zip \
            unzip

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept Android licenses and install build tools
        run: |
          yes | sdkmanager --licenses || true
          sdkmanager "build-tools;33.0.2" "platforms;android-33" "platform-tools" "tools"

      - name: Install buildozer and cython
        run: |
          pip install buildozer==1.5.0 cython==0.29.33

      - name: Configure buildozer to use existing SDK
        run: |
          mkdir -p ~/.buildozer/android/platform
          # 설치된 SDK 경로를 buildozer가 사용하도록 심볼릭 링크 생성
          ln -sf $ANDROID_SDK_ROOT ~/.buildozer/android/platform/android-sdk
          # 라이선스 파일 복사
          mkdir -p ~/.buildozer/android/platform/android-sdk/licenses
          yes | sdkmanager --licenses || true

      - name: Build APK
        run: |
          buildozer -v android debug

      - name: Find APK
        run: find . -name "*.apk" -type f

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SmartMarketingHub-APK
          path: bin/*.apk
          retention-days: 30
